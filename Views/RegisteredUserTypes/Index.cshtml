@model IEnumerable<Dertrix.Models.RegisteredUserType>

@{
    ViewBag.Title = "Index";
}

<h2>User Types</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table table-striped table-bordered table-hover">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RegisteredUserTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RegisteredUserTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RegisteredUserTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.RegisteredUserTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RegisteredUserTypeId })
        </td>
    </tr>
}

</table>

<div>
	<p>@Html.ActionLink("Back", "SysAdminSetUp", "Home", "", new { @class = "btn btn-primary" })</p>
</div>
