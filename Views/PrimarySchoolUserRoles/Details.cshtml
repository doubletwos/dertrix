@model Zeus.Models.PrimarySchoolUserRole

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>PrimarySchoolUserRole</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.RoleName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.RoleName)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.PrimarySchoolUserRoleID }) |
    @Html.ActionLink("Back to List", "Index")
</p>
