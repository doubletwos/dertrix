@model Zeus.Models.PostTopic

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>PostTopic</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.PostTopicName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.PostTopicName)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.PostTopicId }) |
    @Html.ActionLink("Back to List", "Index")
</p>
